import { HightConverterPipe } from './hight-converter.pipe';

describe('HightConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new HightConverterPipe();
    expect(pipe).toBeTruthy();
  });
});

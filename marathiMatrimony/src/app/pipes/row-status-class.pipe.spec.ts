import { RowStatusClassPipe } from './row-status-class.pipe';

describe('RowStatusClassPipe', () => {
  it('create an instance', () => {
    const pipe = new RowStatusClassPipe();
    expect(pipe).toBeTruthy();
  });
});
